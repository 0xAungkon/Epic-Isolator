# Docker Environment Variables for Epic Isolator
# These variables will be used when running the application in a Docker container

# Project information
PROJECT_NAME=Epic Isolator

# API settings
API_V1_STR=/api/v1

# Security
SECRET_KEY=docker_development_key_change_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Database - note paths are inside the container
DB_PATH=/root/epic-isolator/data/epic.db

# File paths - note paths are inside the container
BACKUP_DIR=/root/epic-isolator/backups
LOG_DIR=/root/epic-isolator/logs

# Docker settings
# No need to set DOCKER_HOST as we're mapping the socket directly

# Resource limits (defaults)
DEFAULT_CPU_LIMIT=1.0
DEFAULT_MEMORY_LIMIT=512